db:
  container_name: db
  image: ckan/postgresql
  # ports:
  #   - "5432:5432"
solr:
  container_name: solr
  build: ./solr
  ports:
    - "8983:8983"
ckan:
  container_name: ckan
  build: ./ckan
  ports:
    - "80:80"
  links:
    - solr:solr
    - db:db
